FRONTEND BATTLE PHASE 1 — MASTER BUILD PROMPT
ROLE
You are a Senior Frontend Architect, Performance Engineer, Motion Designer, and SEO Specialist combined. You are building a hackathon submission scored against a published 100-point rubric by senior engineers who will have Chrome DevTools open during evaluation. Every decision is a point decision. Build as if Vercel, Stripe, and Google are reviewing your code.

HARD RULES — DISQUALIFICATION IF BROKEN
Never install framer-motion, @radix-ui, @headlessui, shadcn, react-spring, GSAP, or any animation library.
Never hardcode a single price string in JSX. Every price must flow through computePrice().
Never put currency or cycle state at a level that causes Navbar, Hero, Testimonials, Footer, or FAQ to re-render.
Never animate width, height, top, or left. Use only transform and opacity.
Never exceed 500ms total entry animation orchestration.
Never nest more than 2 non-semantic divs consecutively.
Never leave the GitHub repo private or the Vercel deployment broken.

TECH STACK
Framework: React 18 + Vite
Styling: Tailwind CSS v3 + custom CSS variables
Animations: Native CSS transitions and @keyframes only. WAAPI only if absolutely necessary.
Fonts: Google Fonts loaded via link tag in index.html. Never via npm.
No external icon libraries. Use only the provided SVG files.

ASSETS
Colors — use only these, zero deviation
--clr-oceanic-noir:          #172B36
--clr-nocturnal-expedition:  #114C5A
--clr-mystic-mint:           #D9E8E2
--clr-arctic-powder:         #F1F6F4
--clr-forsythia:             #FFC801
--clr-deep-saffron:          #FF9932
--clr-forsythia-10:          rgba(255,200,1,0.10)
--clr-forsythia-20:          rgba(255,200,1,0.20)
Fonts — load both via Google Fonts preconnect in index.html
JetBrains Mono — weights 400 500 600 700 — use for all headings, price figures, eyebrows, code blocks
Inter          — weights 300 400 500 600 700 — use for all body text, nav links, UI labels
Google Fonts link tag:
https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap
SVG Files — use every single one, comment placement in code
arrow-path.svg          → Navbar refresh badge + Hero automation floating card
arrow-trending-up.svg   → Hero floating metric card + Social proof stats row
chart-pie.svg           → Features bento card "Usage Intelligence" + Social proof
chevron-down.svg        → Currency dropdown indicator + Accordion closed state
chevron-up.svg          → Accordion open state
chevron-up-solid.svg    → Scroll-to-top fixed button bottom-right
chevron-left.svg        → Testimonials carousel previous button
chevron-right.svg       → Testimonials carousel next button
cog-8-tooth.svg         → Features bento card "Smart Configuration"
cube-16-solid.svg       → Logo mark in Navbar and Footer
link-solid.svg          → Features bento card "Deep Integrations"
link.svg                → Footer nav link decoration
search.svg              → Navbar search trigger button
x-mark.svg              → Mobile nav close button + Search dismiss
Create src/assets/svgs/index.js that exports all 14 SVGs as React components using currentColor so they respond to CSS color inheritance.

PROJECT STRUCTURE
nexus-ai/
├── index.html
├── public/
│   ├── favicon.svg
│   ├── robots.txt
│   └── sitemap.xml
├── tailwind.config.js
├── vite.config.js
├── package.json
└── src/
    ├── main.jsx
    ├── App.jsx
    ├── assets/
    │   └── svgs/
    │       └── index.js
    ├── styles/
    │   ├── globals.css
    │   └── animations.css
    ├── data/
    │   ├── pricingMatrix.js
    │   └── featuresData.js
    ├── hooks/
    │   ├── useBreakpoint.js
    │   └── useScrollY.js
    ├── utils/
    │   └── pricingCalc.js
    └── components/
        ├── Navbar.jsx
        ├── Hero.jsx
        ├── TrustedBy.jsx
        ├── Features.jsx
        ├── WorkflowTimeline.jsx
        ├── Pricing.jsx
        ├── Testimonials.jsx
        ├── FAQ.jsx
        ├── CTABanner.jsx
        ├── Footer.jsx
        └── ui/
            ├── PricingCard.jsx
            └── ScrollToTop.jsx

FEATURE 1 — MATRIX PRICING ENGINE (30 points)
src/data/pricingMatrix.js
Store all base prices in USD cents to avoid float precision errors.
jsexport const PRICING_MATRIX = {
  tiers: {
    starter: {
      baseUSDCents: 2900,
      name: 'Starter',
      tagline: 'For individuals and side projects',
      features: ['5 automated pipelines','50K tasks per month','Basic analytics','Email support','99.5% uptime SLA'],
      cta: 'Start Free Trial',
      highlighted: false,
    },
    pro: {
      baseUSDCents: 7900,
      name: 'Pro',
      tagline: 'For growing engineering teams',
      features: ['Unlimited pipelines','2M tasks per month','Real-time analytics','Priority support','99.9% uptime SLA','Custom integrations'],
      cta: 'Start Free Trial',
      highlighted: true,
    },
    enterprise: {
      baseUSDCents: 19900,
      name: 'Enterprise',
      tagline: 'For large organizations',
      features: ['Unlimited everything','Dedicated infrastructure','White-glove onboarding','24/7 dedicated support','99.99% uptime SLA','SSO and SAML','Custom contracts'],
      cta: 'Contact Sales',
      highlighted: false,
    },
  },
  cycles: {
    monthly: { multiplier: 1.0,  badge: null },
    annual:  { multiplier: 0.80, badge: 'Save 20%' },
  },
  currencies: {
    USD: { symbol: '$', tariff: 1.000,  locale: 'en-US' },
    INR: { symbol: '₹', tariff: 83.50, locale: 'en-IN' },
    EUR: { symbol: '€', tariff: 0.920,  locale: 'de-DE' },
  },
};
src/utils/pricingCalc.js
Pure function. No React imports. No side effects. Fully testable in isolation.
jsimport { PRICING_MATRIX } from '../data/pricingMatrix';

export function computePrice(tier, currency, cycle) {
  const { baseUSDCents }           = PRICING_MATRIX.tiers[tier];
  const { multiplier, badge }      = PRICING_MATRIX.cycles[cycle];
  const { symbol, tariff, locale } = PRICING_MATRIX.currencies[currency];

  const raw    = (baseUSDCents / 100) * multiplier * tariff;
  const amount = new Intl.NumberFormat(locale, {
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(raw);

  return {
    symbol,
    amount,
    period: cycle === 'annual' ? '/mo, billed annually' : '/month',
    badge,
  };
}
State Isolation Architecture — Critical for 15 points
The evaluator opens Chrome DevTools Performance tab and records while toggling currency and billing cycle. Any component that repaints outside the price span elements loses points immediately.
Pattern to follow exactly:
In Pricing.jsx use useRef for currency and cycle state, not useState. This means changing them never triggers a React re-render. Update the DOM by writing directly to textContent on id-tagged span elements inside each PricingCard.
In PricingCard.jsx wrap the entire component in React.memo. The component receives initialPrice as a prop and renders once at mount. It never receives updated props. Price changes happen only via direct DOM writes from the parent Pricing.jsx using document.getElementById targeting these exact span IDs:
price-symbol-{tierId}
price-amount-{tierId}
price-period-{tierId}
price-badge-{tierId}
Each price span must have aria-live="polite" and aria-atomic="true" for accessibility.
The badge span starts with opacity 0 and transitions to opacity 1 in 150ms ease-out when badge text is written.
The billing toggle buttons use data-active attribute updated via direct DOM setAttribute. Style them with CSS attribute selectors: button[data-active="true"].

FEATURE 2 — BENTO TO ACCORDION WITH CONTEXT LOCK (10 points)
src/hooks/useBreakpoint.js
Use ResizeObserver on document.body, not window resize event. Fires at 768px threshold. Returns boolean true when mobile.
jsimport { useState, useEffect } from 'react';

export function useBreakpoint() {
  const [isMobile, setIsMobile] = useState(() => window.innerWidth < 768);

  useEffect(() => {
    const observer = new ResizeObserver((entries) => {
      for (const entry of entries) {
        setIsMobile(entry.contentRect.width < 768);
      }
    });
    observer.observe(document.body);
    return () => observer.disconnect();
  }, []);

  return isMobile;
}
src/components/Features.jsx — Context Lock Logic
This is the most architecturally complex component. Follow this exactly.
Use activeIndexRef as a useRef to track which bento card the user is hovering. Never store this in useState because it must not cause re-renders.
Use accordionOpen as useState because it controls accordion render.
Use prevIsMobileRef to track the previous breakpoint state. This prevents the context transfer from firing repeatedly — it must fire only once when crossing from desktop to mobile.
jsconst activeIndexRef  = useRef(null);
const [accordionOpen, setAccordionOpen] = useState(null);
const isMobile        = useBreakpoint();
const prevIsMobileRef = useRef(isMobile);

useEffect(() => {
  const crossedToMobile = !prevIsMobileRef.current && isMobile;
  if (crossedToMobile && activeIndexRef.current !== null) {
    setAccordionOpen(activeIndexRef.current);
  }
  prevIsMobileRef.current = isMobile;
}, [isMobile]);

const handleBentoEnter = useCallback((id) => {
  activeIndexRef.current = id;
}, []);
Do not clear activeIndexRef on mouse leave. It must persist so the context lock works even if the user moved their mouse away before resizing.
On desktop render a bento grid. On mobile render an accordion. Both use the same FEATURES data array.
Bento grid uses CSS grid-template-areas for asymmetric layout. Six cards total.
Accordion uses aria-expanded, aria-controls, and role="region" on every panel.
Accordion CSS — no JS height measurement
css.accordion-panel {
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transition: max-height 350ms ease-in-out, opacity 300ms ease-in-out;
}

.accordion-panel--open {
  max-height: 500px;
  opacity: 1;
}

.accordion-chevron {
  transition: transform 150ms ease-out;
}

.accordion-chevron.open {
  transform: rotate(180deg);
}

.bento-card {
  transition: transform 180ms ease-out, box-shadow 180ms ease-out, border-color 180ms ease-out;
  will-change: transform;
}

.bento-card:hover {
  transform: scale(1.025) translateZ(0);
  box-shadow: 0 20px 40px rgba(23,43,54,0.25), 0 0 0 1px rgba(255,200,1,0.2);
  border-color: rgba(255,200,1,0.2);
}

ANIMATION SYSTEM — src/styles/animations.css
All entry animations must complete within 500ms of page load. Stagger math: last stagger delay 240ms plus duration 300ms equals 540ms which is too slow. Cap stagger at 4 levels maximum with delays 0ms, 80ms, 160ms, 240ms.
css@keyframes fade-up {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}

@keyframes fade-in {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50%       { transform: translateY(-8px); }
}

@keyframes glow-pulse {
  0%, 100% { box-shadow: 0 0 20px rgba(255,200,1,0.10); }
  50%       { box-shadow: 0 0 40px rgba(255,200,1,0.20); }
}

@keyframes marquee-scroll {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}

.anim-hero-1 { animation: fade-up 300ms ease-out 0ms   both; }
.anim-hero-2 { animation: fade-up 300ms ease-out 80ms  both; }
.anim-hero-3 { animation: fade-up 300ms ease-out 160ms both; }
.anim-hero-4 { animation: fade-up 300ms ease-out 240ms both; }

.float-1 { animation: float 4s ease-in-out 0.0s  infinite; }
.float-2 { animation: float 4s ease-in-out 0.8s  infinite; }
.float-3 { animation: float 4s ease-in-out 1.6s  infinite; }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}

COMPLETE index.html
html<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />

  <title>Nexus AI — Intelligent Data Automation Platform</title>
  <meta name="description" content="Nexus AI automates your data pipelines with AI-driven intelligence. Real-time analytics, smart configuration, and enterprise integrations for engineering teams of all sizes." />
  <meta name="keywords" content="AI automation, data pipeline, real-time analytics, workflow automation, SaaS platform, data engineering" />
  <meta name="author" content="Nexus AI" />
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large" />
  <meta name="theme-color" content="#172B36" />

  <link rel="canonical" href="https://nexus-ai.vercel.app/" />

  <meta property="og:type"         content="website" />
  <meta property="og:site_name"    content="Nexus AI" />
  <meta property="og:url"          content="https://nexus-ai.vercel.app/" />
  <meta property="og:title"        content="Nexus AI — Intelligent Data Automation Platform" />
  <meta property="og:description"  content="Automate the chaos. Ship the future. AI-powered pipelines for modern engineering teams." />
  <meta property="og:image"        content="https://nexus-ai.vercel.app/og-image.png" />
  <meta property="og:image:width"  content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:locale"       content="en_US" />

  <meta name="twitter:card"        content="summary_large_image" />
  <meta name="twitter:site"        content="@nexusai" />
  <meta name="twitter:title"       content="Nexus AI — Intelligent Data Automation Platform" />
  <meta name="twitter:description" content="Automate the chaos. Ship the future." />
  <meta name="twitter:image"       content="https://nexus-ai.vercel.app/og-image.png" />

  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "Nexus AI",
    "applicationCategory": "BusinessApplication",
    "operatingSystem": "Web",
    "description": "AI-powered data automation platform for engineering teams.",
    "url": "https://nexus-ai.vercel.app/",
    "offers": {
      "@type": "AggregateOffer",
      "lowPrice": "29",
      "highPrice": "199",
      "priceCurrency": "USD"
    }
  }
  </script>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.jsx"></script>
</body>
</html>

public/robots.txt
User-agent: *
Allow: /
Sitemap: https://nexus-ai.vercel.app/sitemap.xml
public/sitemap.xml
xml<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://nexus-ai.vercel.app/</loc>
    <lastmod>2026-06-26</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>

globals.css — src/styles/globals.css
css:root {
  --clr-oceanic-noir:          #172B36;
  --clr-nocturnal-expedition:  #114C5A;
  --clr-mystic-mint:           #D9E8E2;
  --clr-arctic-powder:         #F1F6F4;
  --clr-forsythia:             #FFC801;
  --clr-deep-saffron:          #FF9932;
  --clr-forsythia-10:          rgba(255,200,1,0.10);
  --clr-forsythia-20:          rgba(255,200,1,0.20);

  --font-display: 'JetBrains Mono', monospace;
  --font-body:    'Inter', sans-serif;

  --text-hero: clamp(2.8rem, 6vw,   4rem);
  --text-5xl:  clamp(2.4rem, 5vw,   3rem);
  --text-4xl:  clamp(2rem,   4vw,   2.25rem);
  --text-3xl:  clamp(1.6rem, 3vw,   1.875rem);
  --text-2xl:  clamp(1.35rem,2.5vw, 1.5rem);
  --text-xl:   clamp(1.15rem,2vw,   1.25rem);
  --text-lg:   clamp(1.05rem,1.8vw, 1.125rem);
  --text-base: clamp(0.95rem,1.5vw, 1rem);
  --text-sm:   clamp(0.85rem,1.2vw, 0.875rem);

  --sp-1:  0.5rem;
  --sp-2:  1rem;
  --sp-3:  1.5rem;
  --sp-4:  2rem;
  --sp-6:  3rem;
  --sp-8:  4rem;
  --sp-12: 6rem;
  --sp-16: 8rem;

  --radius-sm:   6px;
  --radius-md:   12px;
  --radius-lg:   20px;
  --radius-xl:   28px;
  --radius-full: 9999px;

  --transition-micro:  150ms ease-out;
  --transition-fast:   180ms ease-out;
  --transition-layout: 350ms ease-in-out;

  --max-w: 1280px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; }
img, svg { display: block; max-width: 100%; }
button { cursor: pointer; border: none; background: none; font: inherit; }
a { color: inherit; text-decoration: none; }
ul, ol { list-style: none; }

body {
  font-family: var(--font-body);
  font-size: var(--text-base);
  background-color: var(--clr-oceanic-noir);
  color: var(--clr-arctic-powder);
  line-height: 1.6;
  overflow-x: hidden;
}

.container {
  width: 100%;
  max-width: var(--max-w);
  margin-inline: auto;
  padding-inline: var(--sp-4);
}

:focus-visible {
  outline: 2px solid var(--clr-forsythia);
  outline-offset: 3px;
  border-radius: var(--radius-sm);
}

tailwind.config.js
jsexport default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: {
    extend: {
      colors: {
        'oceanic-noir':          '#172B36',
        'nocturnal-expedition':  '#114C5A',
        'mystic-mint':           '#D9E8E2',
        'arctic-powder':         '#F1F6F4',
        'forsythia':             '#FFC801',
        'deep-saffron':          '#FF9932',
      },
      fontFamily: {
        display: ['JetBrains Mono', 'monospace'],
        body:    ['Inter', 'sans-serif'],
      },
      maxWidth: { site: '1280px' },
    },
  },
  plugins: [],
};

SEMANTIC HTML STRUCTURE — enforce throughout
body
  header role="banner"
    nav aria-label="Main navigation"
  main id="main-content"
    section id="hero"          aria-labelledby="hero-heading"
    section id="trusted-by"    aria-label="Trusted companies"
    section id="features"      aria-labelledby="features-heading"
    section id="workflow"      aria-labelledby="workflow-heading"
    section id="pricing"       aria-labelledby="pricing-heading"
    section id="testimonials"  aria-labelledby="testimonials-heading"
    section id="faq"           aria-labelledby="faq-heading"
    section id="cta-banner"    aria-label="Call to action"
  footer role="contentinfo"
    nav aria-label="Footer navigation"
Heading hierarchy — enforced, never break this:
h1 — Hero only. Exactly one in the entire document.
h2 — Section headings: Features, Pricing, Testimonials, FAQ, CTA
h3 — Card and item titles: bento cards, pricing tier names, FAQ questions
h4 — Sub-items only if needed

PAGE SECTIONS — FULL SPEC
Navbar
Background: clr-oceanic-noir. Position sticky top-0. On scroll past 10px add backdrop-filter blur(16px) and a 1px bottom border in forsythia-10 via a CSS class toggled by useScrollY hook.
Logo: cube-16-solid.svg inline + text "NEXUS" in JetBrains Mono forsythia color followed by "AI" in arctic-powder.
Nav links: Features, Pricing, Integrations, Docs. Inter font. On hover color transitions to forsythia in 150ms.
search.svg button: on click reveals an inline search input that slides open 200ms ease-out. x-mark.svg dismisses it.
CTA button: "Get Started". Background forsythia. Text oceanic-noir. On hover scale(1.03) 150ms ease-out.
Mobile: hamburger icon (three CSS lines, no SVG needed). On click slides down a full-width nav panel 300ms ease-in-out. x-mark.svg closes it. aria-expanded on the toggle button.
Hero
Background: clr-oceanic-noir with a radial-gradient bleed using clr-nocturnal-expedition at center.
Eyebrow text: "AI-POWERED AUTOMATION PLATFORM". JetBrains Mono. clr-forsythia. Letter-spacing 0.15em. font-size text-sm. Apply anim-hero-1.
h1: "Automate the Chaos. Ship the Future." font-display hero size white. Apply anim-hero-2.
Subheading paragraph: "Connect any data source, build intelligent pipelines, and ship features 10x faster without the infrastructure overhead." Inter. clr-mystic-mint. Apply anim-hero-3.
CTA row: Primary button "Start Free Trial" forsythia bg oceanic-noir text. Secondary button "Watch Demo" transparent bg forsythia border forsythia text. Apply anim-hero-4.
Three floating metric badges using float-1 float-2 float-3 classes:

arrow-trending-up.svg + "2.3M tasks/day"
arrow-path.svg + "99.9% uptime"
chart-pie.svg + "140ms avg latency"

Each badge: glass effect background, rounded-xl, border forsythia-10.
TrustedBy
Background: clr-nocturnal-expedition. Text: "Trusted by engineering teams at". Six company names in a marquee strip using CSS animation marquee-scroll. Duplicate the names so the loop is seamless.
Features
Background: clr-arctic-powder. Text clr-oceanic-noir.
Eyebrow: "CAPABILITIES". h2: "Everything your data pipeline needs."
Desktop bento grid: 6 cards in asymmetric CSS grid-template-areas layout. Cards span different column and row sizes for visual interest. Hover effect: scale(1.025) translateZ(0), box-shadow with forsythia-20, border-color forsythia-20, 180ms ease-out.
Mobile accordion: full spec in Feature 2 section above.
Feature cards data — use these titles and SVG pairings:

arrow-path.svg — "Automated Pipelines" — "Build, deploy, and monitor intelligent data flows without writing infrastructure code."
arrow-trending-up.svg — "Real-time Analytics" — "Stream processing with sub-150ms latency across all your data sources."
chart-pie.svg — "Usage Intelligence" — "Understand exactly where compute is spent with granular attribution dashboards."
cog-8-tooth.svg — "Smart Configuration" — "Declarative pipeline config that adapts automatically to your data schema."
link-solid.svg — "Deep Integrations" — "Connect to 200 plus services with pre-built connectors and a universal API."
cube-16-solid.svg — "3D Data Modeling" — "Visualize complex data relationships in three-dimensional space."

WorkflowTimeline
Background: clr-oceanic-noir. Eyebrow: "HOW IT WORKS". h2: "From raw data to production in four steps."
Four steps connected by chevron-right.svg on desktop, vertical with chevron-down.svg on mobile:

Connect — link-solid.svg — "Connect your data sources via API or pre-built connectors."
Configure — cog-8-tooth.svg — "Define your pipeline logic with declarative YAML or our visual editor."
Automate — arrow-path.svg — "Nexus AI runs, monitors, and self-heals your pipelines continuously."
Analyze — chart-pie.svg — "Explore outcomes in real-time across every dimension of your data."

Step numbers in JetBrains Mono forsythia color. Large numerals.
Pricing
Background: clr-nocturnal-expedition. Eyebrow: "PRICING". h2: "Transparent pricing, zero surprises."
Full pricing island architecture described in Feature 1 section above.
Three cards: Starter, Pro (highlighted with forsythia border and "Most Popular" badge), Enterprise.
Billing toggle: Monthly / Annual pill. Annual shows "Save 20%" badge that fades in 150ms.
Currency selector: custom styled select using chevron-down.svg. Options USD, INR, EUR.
Testimonials
Background: clr-mystic-mint. Text: clr-oceanic-noir. h2: "Trusted by 10,000 plus engineering teams."
Three testimonial cards. chevron-left.svg and chevron-right.svg navigation. Cards slide with CSS transform translateX transition 300ms ease-in-out.
Each card: quote text, name in JetBrains Mono, role and company in Inter, five-star rating in forsythia.
FAQ
Background: clr-arctic-powder. Text clr-oceanic-noir. h2: "Frequently asked questions."
Six questions using same accordion CSS pattern as Features mobile view. aria-expanded, aria-controls, role="region" on every panel.
Questions to use:

"How does Nexus AI handle data security and compliance?"
"Can I connect my existing databases and tools?"
"What happens if I exceed my monthly task limit?"
"Is there a free trial available?"
"How does annual billing work?"
"Do you offer custom enterprise contracts?"

CTABanner
Background: linear-gradient(135deg, clr-deep-saffron, clr-forsythia). Text: clr-oceanic-noir.
h2: "Ready to automate everything?" Large, bold, JetBrains Mono. Button: "Start Free Trial" oceanic-noir bg arctic-powder text. Hover scale(1.03) 150ms ease-out.
Footer
Background: clr-oceanic-noir. Four columns: Brand, Product, Company, Connect.
Brand column: cube-16-solid.svg logo + "NEXUS AI" + short description + copyright line using new Date().getFullYear().
Product links: Features, Pricing, Integrations, Changelog, Status. Each decorated with link.svg inline.
Company links: About, Blog, Careers, Press, Legal.
Connect links: Twitter, GitHub, LinkedIn, Discord.
Border-top: 1px solid forsythia-10.
ScrollToTop
Fixed position bottom-right. chevron-up-solid.svg. Appears after scrolling 400px. Fade in and out 150ms ease-out. Background forsythia. Border-radius full.

BUILD ORDER — follow exactly, generate every file completely
1.  index.html
2.  public/robots.txt
3.  public/sitemap.xml
4.  public/favicon.svg
5.  tailwind.config.js
6.  vite.config.js
7.  package.json
8.  src/styles/globals.css
9.  src/styles/animations.css
10. src/assets/svgs/index.js
11. src/data/pricingMatrix.js
12. src/data/featuresData.js
13. src/utils/pricingCalc.js
14. src/hooks/useBreakpoint.js
15. src/hooks/useScrollY.js
16. src/components/ui/PricingCard.jsx
17. src/components/ui/ScrollToTop.jsx
18. src/components/Navbar.jsx
19. src/components/Hero.jsx
20. src/components/TrustedBy.jsx
21. src/components/Features.jsx
22. src/components/WorkflowTimeline.jsx
23. src/components/Pricing.jsx
24. src/components/Testimonials.jsx
25. src/components/FAQ.jsx
26. src/components/CTABanner.jsx
27. src/components/Footer.jsx
28. src/App.jsx
29. src/main.jsx
Generate every file fully. No placeholders. No TODO comments. No incomplete sections.

INSTALL AND RUN COMMANDS
bashnpm create vite@latest nexus-ai -- --template react
cd nexus-ai
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm run dev

npm run build
npx vercel --prod

PRE-SUBMISSION VERIFICATION — check every item before submitting
Open Chrome DevTools Performance tab. Record while toggling billing cycle and currency. Confirm zero repaints outside the price span elements.
Open Chrome DevTools Performance tab. Record page load. Confirm all entry animations complete within 500ms of TTI.
Resize from 900px to 600px while hovering a bento card. Confirm accordion opens at the same index.
Run Lighthouse in incognito. Targets: Performance 90 plus, Accessibility 95 plus, Best Practices 95 plus, SEO 100.
Confirm all 14 SVGs are visible on the page.
Run document.querySelectorAll('h1').length in console. Must return 1.
Check zero console errors or warnings in production build.
Check no horizontal scrollbar at 375px viewport.
Confirm keyboard tab order is logical throughout the entire page.
Confirm all images have alt attributes and all icon buttons have aria-label.
Confirm GitHub repository is public and contains actual source code.
Confirm Vercel deployment returns 200 and all assets load correctly.

TIMING LAW — never violate
Entry animations (hero stagger):    500ms maximum total. CSS keyframes only.
Micro-interactions (hover, toggle): 150ms to 200ms. ease-out.
Layout reflows (accordion, panels): 300ms to 400ms. ease-in-out.
Currency and cycle DOM writes:      Synchronous. No delay. No animation.
Accordion chevron rotation:         150ms ease-out.
Navbar scroll effect:               150ms ease-out.
Button hover scale:                 150ms ease-out.
Testimonial slide:                  300ms ease-in-out.
Mobile nav slide:                   300ms ease-in-out.